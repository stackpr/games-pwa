{
  "name": "games-pwa-tests",
  "private": true,
  "description": "Playwright suite for games-pwa. Repo-only: never published to GitHub Pages.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
